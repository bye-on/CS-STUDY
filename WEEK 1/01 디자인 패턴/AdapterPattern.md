# 어댑터(Adapter) 패턴

### 의도

호환되지 않는 인터페이스를 가진 클래스를 **클라이언트가 기대하는 표준 인터페이스**로 변환.
👉 서로 “맞지 않는 인터페이스(접속 방식)”를 가진 클래스들을 연결해주는 중간 통역사 같은 역할

**언제 쓰나**

- 레거시/서드파티 클래스를 신규 코드베이스 인터페이스로 맞춰 붙일 때
- 인터페이스 차이로 직접 사용이 불가능할 때

---

### 상황 예시

**클라이언트 코드(내 코드)** 는 어떤 “인터페이스”를 기대.
예: Shape 인터페이스 안에 draw()라는 메서드가 있다고 가정.

그런데 **이미 만들어진 클래스(레거시 코드)** 는 그 인터페이스를 따르지 않음.
예: LegacyRectangle 클래스는 render()라는 이름으로만 그림을 그릴 수 있음.

이럴 때, 기존 코드를 뜯어고치지 않고
**“중간에서 번역해주는 클래스(Adapter)”** 를 만들어서 연결.

**종류**

- **객체 어댑터**(composition): 권장. 런타임에 교체 쉬움.
- **클래스 어댑터**(inheritance): Java에선 단일 상속 제한 탓에 제약 큼.

### 코드 예제

```java
// Target
interface Shape {
    void draw(int x, int y, int w, int h);
}

// Adaptee (레거시)
class LegacyRectangle {
    public void render(int x, int y, int x2, int y2) {
        System.out.printf("Legacy render (%d,%d)-(%d,%d)%n", x, y, x2, y2);
    }
}

// Adapter (객체 어댑터)
class RectangleAdapter implements Shape {
    private final LegacyRectangle adaptee = new LegacyRectangle();
    @Override
    public void draw(int x, int y, int w, int h) {
        adaptee.render(x, y, x + w, y + h);
    }
}

public class Demo {
    public static void main(String[] args) {
        Shape rect = new RectangleAdapter();
        rect.draw(10, 20, 100, 50);
    }
}
```

| 구분                        | 역할                                         | 예시               |
| --------------------------- | -------------------------------------------- | ------------------ |
| **Target(목표 인터페이스)** | 클라이언트가 기대하는 규칙                   | `Shape`            |
| **Adaptee(기존 클래스)**    | 이미 존재하는, 다른 인터페이스를 가진 클래스 | `LegacyRectangle`  |
| **Adapter(변환기)**         | 둘 사이를 연결해주는 중간자                  | `RectangleAdapter` |

### 핵심 요약

- 목적: 기존 코드를 바꾸지 않고도 새로운 코드와 호환되게 만들기

- 장점: 재사용성↑, 수정 위험↓

- 단점: 어댑터 클래스가 많아지면 구조가 복잡해질 수 있음

**주의/팁**

- 어댑터 내부에서 **단위 변환/예외 매핑/기본값 보정**을 깔끔히 캡슐화
- 과도한 어댑터 남발은 구조를 복잡하게 만듦 -> 퍼사드/래퍼로 단순화 고려
